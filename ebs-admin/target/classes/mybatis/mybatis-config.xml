<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- グローバルパラメータ -->
    <settings>
        <!-- グローバルマッパのキャッシュの有効化または無効化 -->
        <setting name="cacheEnabled"             value="true"   />
        <!-- JDBCがプライマリ・キーの自動生成をサポートできるようにする -->
        <setting name="useGeneratedKeys"         value="true"   />
        <!-- デフォルトのアクチュエータを設定します。SIMPLEは通常のアクチュエータです、REUSEアクチュエータは前処理文を再使用します(prepared statements);BATCHアクチュエータは文を再使用し、一括更新を実行します -->
        <setting name="defaultExecutorType"      value="SIMPLE" />
		<!-- MyBatisで使用するログの具体的な実装を指定します -->
        <setting name="logImpl"                  value="SLF4J"  />
        <!-- ラクダの名前付けを使用したフィールドの変換 -->
		<!-- <setting name="mapUnderscoreToCamelCase" value="true"/> -->
	</settings>
	
</configuration>
